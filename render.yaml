services:
  - type: web
    name: chinese-study-tts
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --workers 2 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
